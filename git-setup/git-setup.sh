#!/bin/bash
git config --global user.name "darkside"
git config --global user.email "raghuram.shankar98@gmail.com"
git config --global credential.helper cache
git config --global credential.helper 'cache --timeout=31536000'
